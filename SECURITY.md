# ClinicaLens 安全与医疗边界

## 医疗权限

- `Assessment.authority` 固定为 `decision_support`。
- Agent 可以整理候选方向、证据、反证、不确定性、紧急度和就医准备。
- `DoctorPlan` 只接受 `hospital`、`sandbox_hospital` 或已经上传的医生文档来源。
- Agent 的 `treatment_plan` 永远不会投影到患者端 `CareJourney`。
- 没有医生来源时，服务拒绝创建复诊、处方、剂量或用药任务。
- 危险信号分流不依赖 LLM；命中时阻断普通分析与挂号路径。

## 身份与授权

- 手机号经服务端 HMAC 后用于用户查找，数据库只保存哈希与掩码。
- 生产环境缺少安全密钥或短信供应商时，验证码入口 fail closed。
- 会话 Cookie 为 `HttpOnly`；生产环境必须启用 `Secure`。
- 所有账户写操作都验证 `X-CSRF-Token`。
- 每条旅程、导入任务、用药和推送订阅都按 `owner_id` 校验所有权。
- `User.role` 只能由服务端配置、后台预置或医院 SSO 写入；患者访问医生 API 固定返回 `403`。
- 病例授权码只保存 HMAC 摘要，十分钟过期且单次使用；患者撤销 `CareTeamLink` 后医生立即失去读取权限。
- 患者 DTO 不含诊断版本、内部候选排序、检查建议或治疗建议；医生 DTO 只对已授权病例生成。
- 医院连接、AI 分析和推送分别记录用户同意状态。

## 数据存储

- 生产结构化数据使用 PostgreSQL；本地 SQLite 只用于开发和测试。
- 生产文档使用 S3 兼容对象存储及服务端加密；公开部署未配置对象存储时禁止上传。
- PWA Service Worker 只缓存应用外壳、完整虚构样例和公开指标，不缓存 `/api/` 响应。
- 用户可导出健康事件，并可删除账户及其数据库记录和文档对象。
- 健康数据默认不用于模型训练。

## 数据最小化

客户端不会收到：

- 原始 Prompt 或完整 Agent 审计日志；
- 模型密钥、医院连接凭证、短信密钥或对象存储凭证；
- 真实医院患者 ID；
- 旧评测兼容接口中的治疗字段。

服务日志禁止记录手机号、真实姓名、原始文档、Token、Prompt 或患者 ID。上传审计只保留文档类型、不可逆摘要和对象键。

## 外部链接与失败模式

- 挂号入口必须使用 HTTPS 且主机在服务端白名单中。
- 产品只记录用户是否完成挂号，不读取或伪造真实号源。
- 医院同步超时会返回明确错误，并提示重试或上传文档。
- Agent 失败时保留病历、医生计划和站内任务，不生成替代诊断。
- 推送失败或被拒绝时，提醒仍显示在患者“治后”页面。

## 公开样例

`/api/sample/*` 只返回人工构造的完整虚构病例、回归指标和白名单运行结果，不含真实患者数据。旧 `/api/demo/*` 为临时兼容别名。公开实时样例仍使用 HMAC 客户端标识、全局/单客户端限流和单任务队列。
